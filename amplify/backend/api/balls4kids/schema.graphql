type Post @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  description: String
  images_url: AWSURL
  tags: String
}

type About @model @auth(rules: [{allow: public}]) {
  id: ID!
  type: String
  text: String
}
